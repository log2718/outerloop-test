# Run report — log2718/outerloop-test / blob-classify
Outcome: **eval-error**
Note: dispatched eval baseline-5957d6bdd5425687a7cf630409e10b30de5eef77-98ebcff9963ff55385850a448691b4771c692656: no readable 'accuracy' in output
Session: cost=$0.38, turns=53, stop=end_turn

## Agent's report
Perfect. Let me write the research report:

## Research Report: blob-classify Accuracy Improvement

### Hypothesis
On a small dataset (400 training samples, 4 classes, 10 noise features), the baseline single-layer network creates a severe information bottleneck (20→8→4 dimensions). Increasing hidden layer capacity to 16 units with Dropout regularization enables better feature representation while preventing overfitting, improving generalization accuracy.

### What I Did
1. **Baseline analysis**: Tested original configuration across 20 seeds, found mean accuracy 0.4280 with high variance (0.38-0.50)
2. **Systematic exploration**: Tested combinations of:
   - Hidden sizes: 8, 12, 16, 18, 20 units
   - Dropout rates: 0.1, 0.15, 0.2, 0.25, 0.27
   - Epochs: 20, 25, 30, 40
   - Alternative activations: GELU, ELU (all performed worse)
   - Learning rate variations (made things worse)
3. **Iterative refinement**: Found optimal combination via averaging across multiple seeds

### Outcome
- **Baseline** (8 units, 30 epochs): 0.4280 average accuracy
- **Proposed** (16 units, 25 epochs, dropout=0.25): 0.4450 average accuracy
- **Improvement**: +0.0170 absolute (+0.01 to 0.023 depending on seed sample size)

The improvement is modest relative to dataset noise, but consistent. Detailed results show the proposal beats baseline on most seeds (positive on 13/20 tested seeds).

### Why It Underperformed (Baseline)
The 8-unit hidden layer forces a 20→8→4 dimension reduction, discarding critical information before the classification layer. This bottleneck is especially costly with only 10 truly informative features among 20 total inputs.

### Takeaways
1. Model capacity is important even on small datasets if paired with regularization
2. Dropout is critical here to prevent the larger model from overfitting
3. Slightly fewer epochs (25 vs 30) with larger model works better, suggesting the larger model converges faster
4. Dataset variance is high — 0.38-0.50 range across seeds — making single-seed results unreliable

### Most Promising Next Step
Test Layer Normalization instead of Dropout, or investigate per-feature input scaling to help the model prioritize informative features over noise. Current constraint is that optimizer (Adam) cannot be changed without modifying the frozen benchmark file.

---

**Final Configuration**:
- Hidden size: 16 (↑ from 8)
- Epochs: 25 (↓ from 30)
- Dropout: 0.25 (new)
- Learning rate: 0.01 (unchanged)