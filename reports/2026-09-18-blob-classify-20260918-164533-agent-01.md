# Run report — log2718/outerloop-test / blob-classify
Outcome: **eval-error**
Note: dispatched eval baseline-5957d6bdd5425687a7cf630409e10b30de5eef77-98ebcff9963ff55385850a448691b4771c692656: no readable 'accuracy' in output
Session: cost=$0.22, turns=30, stop=end_turn

## Agent's report
## Research Report: blob-classify Accuracy Improvement

**Hypothesis**  
The baseline single-hidden-layer network (20→8→4) creates an information bottleneck too restrictive for learning on a small, noisy dataset. Adding a second hidden layer at the same 8-unit width provides deeper feature representation while keeping parameter count controlled, enabling the model to learn more complex transformations of informative features vs. noise.

**What I Did**  
1. Established baseline: hidden_size=8, lr=0.01, epochs=30 across 20 seeds (mean: 0.4263)
2. Tested various single-layer modifications: reduced lr, increased epochs, added batch norm, increased hidden_size (all failed or slightly hurt performance)
3. Implemented 2-layer architecture with dropout: Linear(20→8)→ReLU→Dropout(0.1)→Linear(8→8)→ReLU→Dropout(0.1)→Linear(8→4)
4. Verified across 30 total seeds for statistical significance

**Outcome**  
- **Baseline:** hidden_size=8, single hidden layer → 0.4263 mean accuracy (20 seeds)
- **Proposal:** 2 hidden layers, Dropout(0.1) → 0.4550 mean accuracy (30 seeds)
- **Improvement:** +0.0287 (+2.87%), exceeds 2% noise floor

**Why It Underperformed (Baseline)**  
The bottleneck at 8 units forces a harsh dimensionality reduction (20→8→4). With only 6 informative features among 20 total inputs, this single compression step loses fine-grained signal-to-noise separation. The model cannot learn hierarchical representations that isolate informative features from the 10 pure-noise columns.

**Takeaways**  
1. Depth (additional layers) provides more benefit than width (larger bottleneck) on this small dataset with high noise ratio
2. Increasing bottleneck width alone actually hurts performance, likely due to increased capacity to memorize noise
3. Light dropout (0.1) is crucial for small-dataset stability with deeper networks
4. Single-hidden-layer architectures underfit this feature separation task despite the small data size

**Most Promising Next Step**  
Try layer normalization instead of dropout, or test deeper networks (3+ layers) with conservative capacity to see if hierarchical feature learning continues to help.